# Changelog

## 2.2.2 — 2026-09-19

- **New tab: *Build Update PKG* — like Patch Builder on PS4, built the Sony SDK way.** A PS5 update is not a PS4-style overlay package: Publishing Tools 2.79 has no patch volume type (`prospero_app` / `prospero_ac` / `prospero_al` only); an update is a `prospero_app` with a higher `contentVersion` built with `img_create --ref_pkg_path <base.pkg>`, which writes a block-level delta tied to exactly that base package (digest link). The tab does that with three choices:
  The input is always the base `.pkg` (the one installed on the console) plus the source — the full new game **or just an update folder** with the files you changed or added (`eboot.bin`, `sce_sys`, `fakelib`, translation files…). The app compares file paths with the base package's file list: files in the source are used as they are (*replaced* — keeping the base package's exact name/case — or *added*), every file the source does not have is taken from the base: from the optional *Original game folder* (read only, nothing extracted) or, when that is empty, by extracting just those files from the base package into the temporary folder (free space checked, removed afterwards). Measured: a project that lists only the changed files produces a new version with everything else deleted, which is why the unchanged files must be readable. Choose what to keep:
  - **Update file only** — `UPDATE_….pkg`, the small package to install over the base;
  - **Full game with the update** — `….remastered.pkg`, the whole game at the new version (base + update in one file);
  - **Both**.

  Publishing Tools always writes both; the file that was not chosen is removed after the structure check (the two convert into each other with `img_convert`, so nothing is lost).

  Outputs: `UPDATE_<contentId>-A…-V….pkg` (the small package to install over the base) and `<contentId>-A…-V….remastered.pkg` (the full new version Publishing Tools writes next to it, for checking/extracting). The "output folder already has a package" question never offers the base package for overwrite. A folder without `sce_sys` is fine: Content ID comes from the base and the version is pre-filled one step above it (*Raise the content version to …*). The log and `01-create-gp5.log` list every replaced (`*`) and added (`+`) file; a source that changes nothing stops the build. The patch block left the *Build PKG* tab. CLI: `--sdk-reference <base.pkg>` (+ optional `--sdk-base-folder <dir>`), `--sdk-patch-output update|full|both`, `--sdk-base-folder <dir> --sdk-build-base` to build the base package from the original game folder first and the update against it, `--sdk-patch-exact` to treat the source as the complete new version (missing files = deleted, like the toolkit).
- **Content version and App title are now applied to Sony SDK packages.** The two fields in step 2 are written into the package's `param.json` (`contentVersion`, `titleName` of the default language) when they differ from the source — the source is never touched, and leaving them as read keeps the package byte-identical. The patch check now compares the *Content version field* with the base package, so "contentVersion must be higher" is fixed by typing a higher number (or the new *Raise the content version to …* link) instead of hand-editing `param.json`. CLI: `--version` / `--title` do the same; the queue keeps each source's own values.
- **DLC packages from `dlc_emu.ini`: every `[PSAC]` entry is built** (it used to stop with "Built 0/N" when the entries were not `NO_EXTRA_DATA`). Entries without extra data give an unlock-only package as before; entries with a `mount_point` whose folder exists in the game (`/app0/dlc01` → `<game>/dlc01`) are packed with that data (the game folder is only read); entries with data that cannot be found still give an unlock package plus a warning. `[PSAL]`/`[PSCONS]` sections are skipped with a note.
- **Fixed a possible crash when several builds start at the same time on macOS** (queue): .NET's drive enumeration (`getmntinfo`) is not thread-safe and could take the whole app down with an access violation; drive lookups are now serialised.
- **Update patches (toolkit `sdk-fpkg279-fix8-deltas`).** New optional block in step 1, *Build a patch against an installed base package*: pick the `.pkg` that is installed on the console and Publishing Tools compares the new build with it (`img_create --ref_pkg_path`) and writes only the differences — `<name>.pkg` is the small update to install over the base package, `<name>.pkg.remastered.pkg` is the full companion used for checking/extracting. The app reads the base package as soon as it is chosen and shows its Content ID / version under the field; a different game, or a `contentVersion` that was not raised in `param.json`, is reported right there and stops the build before Publishing Tools runs (the SDK would only fail after compressing everything). The structure check runs on the companion (a delta has no FIH header); the log reminds the install order. Sony SDK only — with the built-in engine, or when the SDK is unavailable, the build stops instead of silently producing a full package. CLI: `--sdk-reference <base.pkg>`. The queue always builds full packages. Bundled toolkit files updated to fix8 (`ric.exe`, `prospero-dds2png.exe`, scripts; `prospero-pub-cmd.exe` is unchanged from fix6). Measured: a 1 KB change in a 7 MB game gives a 1.5 MB patch.
- **More tests for every platform** (323 in total): localisation key/placeholder parity between Vietnamese and English and format-string validity, the bundled toolkit's file list, OS-dependent services never throwing where they do not apply (sleep guard, component probe, Rosetta/VC++/Wine lookups, update asset names), alias folders never on the source drive, output names valid on every file system, plus the patch flow through the real SDK (delta + companion, version not raised, base package of another game, no SDK).
- **Toolkit updated to `sdk-fpkg279-fix6` (profile `sdk279-plaintext-direct-v3`).** The patched `prospero-pub-cmd.exe` now writes the PLAINTEXT_NOAUTH package directly, so the third step (the post-process conversion) is gone: the package is written as `<name>.partial.pkg` and renamed. The C# converter stays as a fallback for packages that come out without the plaintext marker (older toolkits).
- **PlayGo for dumps without `playgo-chunk.dat`, the way the new toolkit script does it**: 100 chunks, game files in chunk 0, and **one chunk per language** listed in the dump's `playgo-scenario.json` (or derived from its localisations), each holding a 1 MiB uncompressed placeholder (`playgo-languages/NN-<lang>.bin`) so the console reports every voice language as installed; the source scenarios, titles and default scenario are kept, every chunk is initial. The GP5, scenario file and `param.json` are byte-identical to the script's output (checked against the bundled Python script on a two-scenario, three-language fixture). An invalid scenario file falls back to the default scenario with a warning instead of stopping. Dumps that still have the original tables (Ghost of Yōtei) keep their real chunk layout and file map as before — the script cannot do that.
- **Like the toolkit, the generated `.gp5`, `.playgo-scenario.json` and `.gp5-assets` are removed after a successful build**; the `-build-logs` folder stays. New option *Keep the GP5 and generated files* / `--sdk-keep-intermediate`.
- **Windows: Publishing Tools' own temporary files go to the app's temporary folder** (TEMP/TMP of the SDK process), so they follow the drive chosen in the app instead of always landing on C:.
- **macOS 27: Rosetta 2 is detected correctly and can be reinstalled from the app.** A major macOS upgrade (macOS 27) removes Rosetta 2 and leaves only `RosettaLinux` in the `oah` folder; the old check took that for an installed Rosetta and Wine then died with "Bad CPU type in executable". The app now requires `libRosettaRuntime`, explains what happened, and the Sony SDK warning has an **Install Rosetta 2** button that runs Apple's `softwareupdate --install-rosetta --agree-to-license` after a confirmation (no admin password), then re-checks the components. Verified on macOS 27.0: the whole suite including the real Publishing Tools builds passes, The Smurfs – Dreams (9.5 GB) builds in 32 s.
- **Build queue — several games at once.** A third mode next to *Build PKG* / *Extract PKG*: drop game folders, disk images or a whole dump drive into the queue (a folder that is not a game itself adds every valid sub-folder), choose how many builds run in parallel (1–4, default 2), press Start. Each entry has its own progress bar, phase, ETA, elapsed time, result path and log (select an entry to read its log); entries can be re-ordered, cancelled, retried or removed individually, the whole queue can be paused (running builds finish, nothing new starts) or cancelled, and finished entries cleared. Each build uses the Build PKG tab's settings at the moment it starts (Sony SDK, DRM, compression level…) and the "Auto from source" output rule; a package with the same name in the output folder is renamed and kept instead of asking. Two entries with the same Content ID into the same output folder never run at the same time (the second is skipped with a note). The window asks before closing while the queue is building.
- **Fixed: "reserved node found (dst_path sce_sys/origin-param.json / target-param.json)" when rebuilding a folder that Extract PKG produced.** Publishing Tools keeps the original and target `param.json` of a patch/backport as `origin-param.json` / `target-param.json` inside the package's CNT, Extract PKG writes them back into `sce_sys/`, and the SDK rejects both names in a GP5 (measured; `nptitle.dat`, `npbind.dat`, `pubtoolinfo.dat`, `changeinfo/`, `*-deltainfo.dat` are accepted). Both files are now skipped like `license.dat`; the toolkit's own script lacks these entries.
- **Fixed: "Format of the png file is not valid (sce_sys/pic2.png)" on folders produced by Extract PKG.** Some packages carry a few hundred bytes of junk under the name `pic2.png` in their CNT; Extract PKG writes it back and Publishing Tools then refuses the whole build. A splash/icon PNG that is not a valid PNG (signature, IHDR, 8-bit RGB/RGBA, non-interlaced) is now left out of the GP5 and rebuilt from the `.dds` copy with prospero-dds2png, exactly like a missing PNG (the log says which file was replaced).
- **Faster "creating the GP5 project" when splash PNGs must be rebuilt from DDS.** The prospero-dds2png conversions now run in parallel (one process each, up to half the CPU cores) instead of one after another; on macOS every 4K BC7 image cost ~5 s through Wine, so a game missing nine `pic2_XX.png` files spent 50 s of a 2-minute build there (The Smurfs – Dreams, 9.5 GB, measured).
- **Parallel builds are isolated**: the Wine prefix is created once even when several SDK builds start together, cancelling one SDK build no longer kills the `wineserver` shared with the others, and every build keeps its own temporary, mirror and ASCII-alias folders. Tests: two SDK builds in parallel, and cancelling one while the other finishes.

## 2.2.1 — 2026-09-18

- **Fixed: "Something went wrong (CE-108111-2)" dialog when launching a game rebuilt with its original PlayGo chunks (Ghost of Yōtei).** The original tables mark only the first 32 of 35 chunks as initial (the rest stream in later on a retail install), so the game asked the system about the "later" chunks at start-up and the system answered with that dialog (the game then continued normally). Every scenario now marks all of its chunks as initial — an FPKG is fully installed, nothing arrives later — while chunk order and per-chunk languages stay as in the original package. LibProsperoPkg 0.6.9 does the same.
- **Engine updated to fpkg-gui 0.6.9** (LibProsperoPkg.dll): language chunks generated from `playgo-scenario.json`, improved PlayGo validation.
- **PS5 Pro Enhanced / 120 Hz / VRR are kept.** "Clear the attribute3 flags in param.json" is now **off by default** (CLI: `--clear-attribute3` to opt in; `--keep-attribute3` still accepted). `attribute3` is the game's feature-flag field — bit 7 = 120 Hz, bit 19 = VRR, bit 23 = PS5 Pro Enhanced (a game supporting all three stores 4456512) — so the old default of zeroing it made PS5 Pro consoles run such games without their Pro enhancements. The zeroing came from an old "uncheck the PlayGo flags" black-screen guide; no public documentation maps any PlayGo bit in attribute3, the real PlayGo problem is solved in the PlayGo tables since 2.1.7/2.2.0, and Sony's own toolkit leaves the field alone. Existing settings are migrated once (`PlayGoDefaultsRevision` 2); the option and the plan log still explain what the bits mean.
- Verified against Publishing Tools: with the option off, `attribute3` (e.g. 4456512) survives the SDK build unchanged in the package's `param.json`. Note on `sce_sys/about/right.sprx`: Publishing Tools rejects the whole `sce_sys/about` directory as a reserved node and does not regenerate it, so SDK-built packages have no `right.sprx` — that module only provides the logo/copyright screen (▲+■+Options on the game's Information page) and has nothing to do with PS5 Pro; the built-in engine keeps it as before.

## 2.2.0 — 2026-09-18

### Verified on the Windows research pass (2026-09-17/18)

- **Ghost of Yōtei voice fix confirmed at the package level.** The dump's original PlayGo table has 35 chunks (29 per-language `pgc_lang_*` chunks, several sharing the same language mask), 1 scenario with `initial_chunk_count = 32`; the game's `eboot.bin` imports `libScePlayGo`. Publishing Tools 2.79 accepts the GP5 written by the app, and a package built from a fixture carrying the real tables reports an identical structure (chunks, masks, labels, scenario, initial count, default language, every file in the same chunk). A synthetic 35-chunk test with duplicate masks now runs through the real SDK. PS5 confirmation still pending.
- **playgo-scenario.json of the original package is reused** for multi-chunk sources when it matches the scenario structure: Publishing Tools copies this file into the package verbatim, and the PlayGo tables do not depend on it.
- **Byte-for-byte fidelity with the toolkit on Windows.** The toolkit's Python script writes the `.gp5`, `.playgo-scenario.json` and the normalized `param.json` with CRLF line endings (text mode on Windows); the app wrote LF, so the scenario file inside the package differed (15 bytes). The app now writes CRLF on every platform. Verified: the same fixture built by the original `build.bat` and by the app gives packages of identical size whose only differing bytes are build timestamps and the digests that depend on them.
- **Much faster SDK builds on Windows: parallel pre-scan.** Windows Defender scans every file on its first open (~11 ms), and `img_create`'s "Checking files to see if compression/conversion is needed" opens files one by one — Ghost of Yōtei (95,386 files) spent about 25 minutes there. The app now opens every source file read-only in parallel before running Publishing Tools (~700 files/s instead of ~90 on this machine), so the SDK's check finds the scan results cached (2,011 fresh files: 22.5 s → 0.4 s). Nothing is written; the GP5 and package are unchanged. On by default; CLI `--no-sdk-prescan`, GUI toggle under Sony SDK options.
- **New opt-in Sony SDK options** (defaults stay identical to the toolkit): compression level for `img_create --compression_level` (-4..9, CLI `--sdk-compression-level`), and **PlayGo fallback** for sources whose dump no longer has `playgo-chunk.dat` (24 of 25 dumps surveyed) — N chunks (`--playgo`, default 100) and the scenario count of the source's `playgo-scenario.json` (The Last of Us Part I/II keep 2 scenarios), like the built-in engine, instead of 1 chunk / 1 scenario (CLI `--sdk-playgo-fallback`).
- **Clear error for file names Publishing Tools rejects.** Measured with `img_create`: every printable ASCII character is accepted except `%` and `;`; any non-ASCII character and names ending with a dot are rejected ("invalid attribute value dst_path"). The app now stops right after writing the GP5 with the list of offending files and how to fix it, instead of letting the SDK fail minutes later.
- **Internet shortcut files (`*.url`) are treated as junk** — dump sites drop files such as `更多资源请访问 2468c.com.url` into `sce_sys` (Frostpunk 2, PRAGMATA), whose non-ASCII names made the SDK reject the whole GP5.
- **Safety: aliases are never created on the source drive.** `SonySdkPathAliases.DefaultBases` still listed the source drive root as a last resort; it is removed.
- **Step 3 presets work with the Sony SDK too**: while "Sony SDK" is on they set `img_create --compression_level` (Smallest = the toolkit's default 7, shown selected by default; Fast 2 / Standard 4 / Maximum 9 opt-in) instead of being greyed out with the built-in engine's level. **PlayGo fallback is on by default** now (24 of 25 surveyed dumps have no `playgo-chunk.dat`); CLI `--no-sdk-playgo-fallback` turns it off.
- **Update check**: test builds (`2.2.0-testN`) are recognised as older than the final `2.2.0`, and a build never offers itself as an update (the tag suffix was stripped before comparing).
- **Linux builds** (`scripts/publish-linux.sh`, x64 and arm64 tarballs with app + `fpkg-cli` + `sony-sdk`). The Sony SDK runs through the distribution's Wine (`/usr/bin/wine`, `wine64`, `/opt/wine-*`, PATH or `PSVIETHOA_WINE`; the WINEPREFIX lives in `~/.local/share/psviethoa-fpkg-builder`), the update check knows the `Linux-x64` / `Linux-arm64` assets, and the machine is kept awake with `systemd-inhibit` when available. Images are extracted with the .NET reader as on Windows without Dokan.
- Tests: 281, now passing on Windows as well (tests written on macOS used file names Windows forbids, `\n` line splitting and symlink creation without privileges).

- **Fixed: no dialogue audio / greyed-out voice languages in games with per-language voice packs (Ghost of Yōtei).** The toolkit's script hard-codes one PlayGo chunk and one scenario, but such games keep each voice language in its own PlayGo chunk and ask the system whether that chunk is installed; with a single chunk the language chunks do not exist, so the game greys the languages out and asks PSN for a download that never starts. The app now reads the original package's `sce_sys/playgo-chunk.dat` (chunk count, per-chunk language masks and labels, scenarios, default language/scenario) and `playgo-ficm.dat` + `playgo-hash-table.dat` (file → chunk map through the PS5 flat-path hash) and writes the same structure into the GP5 — `<chunks supported_languages default_language>`, `<chunk id languages label>`, `<scenario …>` and `chunk="N"` on every file that belonged to a language/extra chunk, exactly as `prospero-pub-cmd gp5_chunk_add / gp5_scenario_add / gp5_file_add --chunk` write them — so Publishing Tools regenerates PlayGo with the original chunks and an installed FPKG has every chunk "installed". Verified round trip on tables produced by Publishing Tools itself (3 chunks with `ja-JP en-US` / `fr-FR` language chunks, 2 scenarios, per-file assignment): the rebuilt package reports the same chunks, languages, scenario order and file map. Sources with a single-chunk table (or none) build exactly as the script does. The log names the chunks kept and how many files were assigned.
- **Toolkit updated to `sdk-fpkg279-fixdss3`** (same SDK binaries; new scripts). The app reproduces the new script byte-for-byte: `sce_sys/license.dat` / `license.info` and `fakelib/libSceAmpr.sprx` / `libScePlayGo.sprx` are excluded by the script itself now; `ampr_emu.index` and a stale `.gp5-assets` folder in the source are skipped; a **normalized `param.json` with `applicationDrmType = "standard"`** is written to `.gp5-assets/<package name>/sce_sys/param.json` next to the GP5 (Python `json.dumps(indent=2)` formatting, reproduced by `PythonJson`) and the GP5 maps `sce_sys/param.json` to it — so the "DRM standard" checkbox shows as ticked and locked while Sony SDK is on, exactly like the toolkit which has no option to keep `free`; the app's extra `versionFileUri` / `attribute3` / required-firmware patches are applied on top. **Missing splash-screen PNGs are recovered**: for every `sce_sys/pic*.dds` without a matching `.png` the bundled `prospero-dds2png.exe` (a standalone win-x64 BC7 decoder, run through Wine on macOS; `--preserve-alpha` for `pic2`) writes the PNG into `.gp5-assets/…/sce_sys/` and the GP5 lists it right after the scenario, like the script. `01-create-gp5.log` carries the script's `Recovering …` lines. Tested end to end: a source with `pic0.dds` only builds a package whose CNT contains `pic0.png` (and the SDK-generated `license.dat`).

- **New default build method: the Sony SDK** (the `sdk-fpkg729-fix` toolchain, profile `sdk279-plaintext-unsigned-v2`). Packages are built by Sony's own Publishing Tools 2.79 command line (`prospero-pub-cmd.exe`, patched to leave the outer PFS plaintext and accept a custom keystone), in the same three steps as the toolchain's `build-from-folder.ps1`:
  1. **GP5 project.** The app writes a flat `<package name>.gp5` that lists every file explicitly, plus a default `<package name>.playgo-scenario.json`, next to the package — **byte-identical to what `create-gp5-from-folder.py --keep-keystone --absolute-paths` produces** for the same folder (verified against the bundled script in the test suite): same XML layout, same `Path.resolve()` paths, same exclusions and, most importantly, the same file order — Python's `str.casefold()` order, reproduced with the CPython folding table (`PythonCaseFold`), because Publishing Tools lays the image out in GP5 order and a different order is a different package. Files the SDK regenerates are left out: PlayGo tables, `pfs-version.dat`, `imagedigs.dat`, `param.sfo`, `sce_sys/about/`, `.dds`/`.auth_info`, old `.gp4`/`.gp5` projects and `ampr_emu.index`. The source's 96‑byte `sce_sys/keystone` is always kept. Symbolic links are rejected like the script does. The GP5 points straight at the source files, so **no mirror folder and no links are needed** (the junction error on exFAT drives cannot happen); the only file the app substitutes is the patched `sce_sys/param.json`, written to the temporary folder.
  2. **Raw package.** `img_create --oformat nwonly` compresses at Sony's default level (7), generates PlayGo and writes the package directly into the output folder.
  3. **Conversion.** The raw package becomes a LibProsperoPkg-compatible `PLAINTEXT_NOAUTH` package: `PPRPLAIN-NOAUTH!` marker, CNT encryption flags cleared, image digests, FIH/CNT cross-references, entry digests and rollups resealed, the deterministic RSA‑3072 CNT header wrap rebuilt, and the PlayGo CRC32C table in the SI archive repaired.

  The conversion is a C# port of `postprocess-sdk279-plaintext.py` that produces **byte-identical** output. It patches the package in place instead of copying it, and nothing needs Python. Like `build-from-folder.ps1`, the `.gp5`, the `.playgo-scenario.json` and a `<package name>-build-logs` folder (`01-create-gp5.log`, `02-img-create.log`, `03-postprocess.log`, same keys as the script) stay next to the package; the raw `.sdk-plaintext.pkg` and `.naps_metric.json` are removed.

  End-to-end check against the original toolkit (Python scripts + Wine) on the same 400 MB test game: identical file list, every extracted file byte-identical, and the packages differ only in the ~2 KB that depend on the build timestamp (`pubtools.creationDate`, digests, CRCs) — the same difference as running the toolkit twice.
- **Runs on macOS too.** Every SDK file is a Windows x86-64 program. The macOS app bundles a trimmed **Wine 11.17** (Gcenx build, LGPL, ~250 MB) and runs the SDK through it; Apple Silicon needs Rosetta 2. The first Sony SDK build creates a private Wine prefix in `~/Library/Application Support/PSVIETHOA FPKG Builder/wine-prefix`, which takes about 30 s; later runs start in about 2 s. On Windows the SDK runs natively and needs Visual C++ 2015-2022 x64. `Setup.exe` installs it silently, and the portable zip installs the bundled `vc_redist.x64.exe` (one UAC prompt) on the first build that needs it.
- **"Sony SDK" checkbox**, on by default, under the progress bar next to "DRM standard". Untick it to build with the built-in engine as before; CLI `--no-sony-sdk`. The source must contain `sce_sys/keystone` of exactly 96 bytes, as the toolchain requires. The build stops with a clear message before any extraction, and the message suggests unticking the box.
- **Options the toolkit does not have are greyed out while Sony SDK is on**, instead of being silently ignored: package type and image mode are forced to *Application* + *PLAINTEXT_NOAUTH*, and the presets, Kraken backend/level/threads, PFS format, block size, shuffles, layout optimisation, PlayGo chunk count, SDK override / required-firmware lowering, deterministic build and source reading mode are disabled with a note. "Drop sce_sys/playgo*" shows as ticked and locked because Publishing Tools always regenerates PlayGo (1 scenario / 1 chunk, like the toolkit). Passcode, DRM "standard", `versionFileUri`/`attribute3`, AMPR/DLC-emu cleanup, OS-junk skipping, SHA-256 and the full check still apply. The checkbox is disabled for `.gp5` sources (the SDK creates its own project), and a banner explains when the SDK cannot run on this computer. The CLI warns which flags are ignored. The disk estimate no longer counts an intermediate image, and the build log no longer prints engine-only lines.
- **Fixed: "Format of the GP5 file is not valid … reserved node found (dst_path "sce_sys/license.dat")".** Dumps extracted from a package (including by this app's Extract mode) carry the fake `sce_sys/license.dat` / `license.info` from the CNT; Publishing Tools regenerates them and rejects them as input, and the original script does not exclude them (its `build.bat` fails on such dumps). Both are now left out of the GP5, with the reason in the log. Every other `sce_sys` name was tested against the SDK: `nptitle.dat`, `npbind.dat`, `pronunciation.xml`, `changeinfo/`, `uds/`, `trophy2/`, `keymap_rp/`, `pic0/pic1.png`, `snd0.at9`, `save_data.png` and unknown files are accepted, so only the script's list plus the two license files are excluded.
- **Fixed: the build (and Cancel) hung for minutes in "[1/3] creating the GP5 project" on a USB drive.** After listing the files, writing the GP5 resolved the real path of every file again (one directory handle per path component, ~500,000 opens on a 95,000-file game over USB) and that loop did not check for cancellation, so Cancel waited for it too. File paths come from the already-resolved source root, so the per-file resolution is gone (95,000 entries now take 0.3 s) and the GP5 writer checks for cancellation every 2,000 entries.
- **Fast file listing for the GP5.** The 95,000-file walk of a real game reads sub-folders in parallel (up to 8 at once — USB/exFAT drives spend most of the time waiting on the disk) and computes each sort key once, while keeping the exact Python order: 95,000 files list in 0.3 s on an SSD (the Python script: 1.6 s) and the GP5 is still byte-identical. The progress line shows "N files listed" while it runs.
- **Folders with non-ASCII names work** ("Ghost of Yōtei", Vietnamese names, CJK). `prospero-pub-cmd.exe` takes its command line in the ANSI code page and rejects non-ASCII `src_path` values in the GP5 ("File not found … Yotei", "Format of the GP5 file is not valid … invalid attribute value src_path") — the original `build.bat` fails the same way. When the source, output or temporary path contains such characters the app now creates temporary ASCII aliases (symbolic links on macOS, NTFS junctions on Windows, in the system temp folder / ProgramData / the output drive, never on the source drive) and hands the SDK every path through them; the links are removed after the build and the real folders are never touched. The log names each alias.
- The bundled toolkit keeps the original layout (`sony-sdk/README.md`, `build.bat`, `build-from-folder.ps1`, `scripts/`, `toolchain/`), so the reference scripts ship with the app and can be run by hand on Windows.
- **Progress bar for the SDK stage.** The SDK's own 51-character `=` bar is read character by character while it runs, and the bytes written to the raw package are shown next to it. Phases: runtime → GP5 → compress & write → convert → verify. Cancel stops the SDK (and the Wine server).
- Every package built through the SDK goes through the same structural and engine checks as before. `DRM "standard"`, AMPR/DLC-emu cleanup, `versionFileUri`/`attribute3` patches and junk skipping are applied through the read-only mirror, as for the built-in engine.
- **Automatic fallback.** When the SDK cannot be used (homebrew/DLC packages, Native image mode, an existing `.gp5` source, or a missing toolchain/Wine/Rosetta), the log says why and the built-in engine builds the package.
- Known difference: the bundled toolchain has no full SDK (`prospero-llvm-readelf.exe`), so Publishing Tools writes `sdkVersion` and `requiredSystemSoftwareVersion` as `0x0000000000000000` in the package's `param.json`, exactly as the original toolchain does on Windows. The log shows the before/after values.
- Components box / `fpkg-cli info`: new **Sony SDK (Publishing Tools 2.79)** row (toolchain folder, Wine version or Visual C++ state). An info note under the presets explains that Kraken/PFS/PlayGo options apply only to the built-in engine.
- Tests: 247, including a real build through Publishing Tools (+ Wine on macOS), the GP5 byte-for-byte comparison with the bundled Python script, Python casefold ordering, symlink rejection, the mirror-free build layout and the SDK-mode disk estimate.

## 2.1.9 — 2026-09-16

- **LibProsperoPkg engine from fpkg-gui 0.6.8.** Same public API (1.2.0) plus a new PlayGo builder, package verification and DRM options.
- **PlayGo is always regenerated by the engine.** The new engine never copies the dump's `playgo-chunk.dat` / `playgo-hash-table.dat` / `playgo-ficm.dat` / `playgo-scenario.json` into the package: every table, the scenario file and the scenario images are rebuilt for the new image ("Fixed an issue with PlayGo map creation"). When the files are kept it only reads the chunk and scenario counts from `playgo-chunk.dat` (or `scenarioCount` from `playgo-scenario.json`). The chunk range grows from 1–64 to **1–255** and the fallback default from 64 to **100**; settings still holding the old default of 64 move to 100 once.
- **Broken PlayGo inputs no longer stop the build.** The engine aborts with "Invalid PS5 playgo-chunk.dat header." when a kept file is malformed. With "Drop old playgo*" off, the tool now checks both files with the engine's own rules (magic `plgx`, version `0x1000`, stored length, 1–255 chunks, 1–64 scenarios) and leaves only the broken ones out, with a warning. The source card shows the counts the engine will use.
- **DRM "standard" is applied by the engine** (`ForceStandardApplicationDrm`) in memory instead of through a patched `param.json`, so forcing DRM alone no longer needs a mirror or overlay. The log still names the original value.
- **`requiredSystemSoftwareVersion` is lowered to the game's SDK** ("Automatic downgrading of the required software version to the SDK-specified version"). When you pick an SDK the engine sets both `sdkVersion` and `requiredSystemSoftwareVersion` to it. When you keep the game's SDK, a new toggle (on by default, CLI `--keep-required-fw` to opt out) lowers `requiredSystemSoftwareVersion` to the major/minor of `sdkVersion` whenever it is higher, e.g. 10.40 → 4.50. It never raises the value, and the source file is not touched.
- **Every build is verified by the engine.** After the structural check, the new quick verification reads the package metadata: segment ranges, CNT signature and entry digests, the complete PlayGo layout, the outer PFS superblock, NAPS, inner PFS inodes and the SI directory. It takes milliseconds to seconds. A package that fails is reported as a failed build instead of a success. New option **Full check after building** (off by default, CLI `--full-verify`) also decrypts every outer block, matches it against `imagedigs.dat` and test-decompresses every file in memory.
- **Quick check / Full check buttons in Extract PKG mode** for any existing package, with progress and cancel. `fpkg-cli verify` runs the same checks (`--full`, `--passcode`) and exits with code 2 when a package fails.
- **Export DLC template** (fpkg-gui 0.6.8). Opening a DLC package with data in Extract PKG mode shows an **Export DLC template** button. It writes the `sce_sys` files needed to rebuild the DLC (`param.json`, `license.dat` / `license.info`, PlayGo, icon) plus a `<Content ID>.gp5` project to a `<package>-dlc-template` folder next to the `.pkg`. The project carries the DLC volume type, Content ID, passcode, the entitlement key read from `license.info`, and the original chunk/scenario layout. The payload is not copied: put the DLC data in the folder and pick the `.gp5` as the source to build the package again. CLI: `fpkg-cli pkg-dlc-template <file.pkg> [--output <dir>]`.
- **Temporary folder on a different drive from the source or output.** Reported on Discord: picking the temporary folder on one drive and the output on another failed with "files cannot be dropped". The mirror folder is built from links, and a directory junction can only live on NTFS, so on Windows a temporary folder on an exFAT/FAT32 external drive (or a network drive) could not hold it and every build stopped. Now:
  - The mirror is only a few links plus `sce_sys`. When the chosen temporary folder cannot hold links, it is built in the system temporary folder instead; the large temporary data still goes to the folder you chose.
  - Junctions are created directly through the Windows API (`FSCTL_SET_REPARSE_POINT`); `mklink /J` remains only as a fallback.
  - Extracted images (`.exfat` extraction, `.ffpfsc`, `.ffpkg`) are the tool's own copy, so files are dropped and `param.json` patched in place, with no links at all.
  - A temporary folder you chose yourself is no longer reset at startup when it sits on a different drive from the output folder.
  - The log names both drives and their file systems, and warns when the temporary or output folder is on FAT/FAT32, which cannot hold the 4 GB+ files a game package needs. The disk-space line in the app shows the same warning.
- **macOS: no more `._*` files inside packages built through an exFAT/FAT drive.** macOS 26 tags every newly created file with `com.apple.provenance`, and exFAT/FAT store that as an AppleDouble `._name` file next to it. A temporary folder on such a drive therefore produced packages containing `sce_sys/._param.json` and a `._` twin for every extracted file. Those AppleDouble files (magic `0x00051607`) are now removed from the mirror and from extracted copies before packaging. Links are never followed, and files that exist in your source are never touched.
- **macOS: temporary folders on exFAT/FAT drives are removed again.** The new FSKit exFAT/FAT driver lists accented names in NFD form but silently ignores a delete by that same name, so an extraction folder containing Vietnamese file names stayed behind ("Directory not empty"). Deletion now retries with the other Unicode normalization form.
- Tests: 220, including real builds that confirm DRM, required-version lowering, SDK override, PlayGo count import, broken-input skipping, quick and full verification (app, homebrew and Native-image packages), and that a single flipped byte fails the full check.

## 2.1.8 — 2026-09-14

- **New source format: `.ffpkg`.** These dumps are a **FreeBSD UFS2 filesystem image** — superblock at byte 65536, magic `0x19540119` at offset 1372, 64 KiB blocks — whose root directory *is* the game's app folder. macOS dropped UFS in 10.7 and Windows never had it, so neither system can mount one; the tool now parses the format itself: superblock, cylinder groups, 256-byte inodes with 12 direct block pointers and three levels of indirection, then the directory records. Pick one with the new **.ffpkg file** button, drag and drop it, or pass it to `fpkg-cli inspect` / `build --source x.ffpkg`. Metadata, icon, statistics, junk-file listing, validation and packaging all behave exactly as they do for `.exfat`; because no OS can mount UFS the image is extracted to the temporary folder using that reader. Verified end to end on a real 3.6 GB dump: 17,407 files and 2,343,308,017 bytes, matching an independent reader written separately to check the numbers.
- **macOS no longer extracts `.exfat` images.** An `hdiutil` mount is read-only, so until now anything that needed files dropped (`playgo*`, OS junk) or `param.json` patched forced a full extraction to the temporary folder. The mirror folder can now be built directly on top of the mounted volume — it only *reads* from the mount and writes changes into the temporary folder — so the image is mounted and read in place, with no copy. OS junk is skipped in the mirror too and gets its own log line.
- The mirror no longer uses `File.Copy`: on macOS that copies extended attributes and throws "Attribute not found" when the source sits on a freshly mounted exFAT volume. It now copies file contents only.
- When the mirror cannot be built the log names the exact file that failed instead of showing a bare error.
- The five source buttons wrap onto a second line when the window is narrow, so their labels are no longer cut off.
- The build log no longer calls every image an "exFAT image".
- Tests: 198.

## 2.1.7 — 2026-09-14

- **"Application error" the moment the game starts ("playgo" error in the PS5 log) — fixed at packaging time, for every source type.** An FPKG is one single unified image while PlayGo is built around chunk-based installs, so the dump's PlayGo data makes the console look for chunks that do not exist. Per Drakmor, dropping those files clears the application error at launch; a game that hangs on the splash screen without an error (Stellar Blade, Ghost of Yōtei) has a different, kernel-side problem that packaging cannot fix. The build now applies the known fix by default: the `sce_sys/playgo*` files (`playgo-chunk.dat`, `playgo-hash-table.dat`, `playgo-ficm.dat`, `playgo-scenario.json`) are left out and the library generates a fresh PlayGo set matching the image; `versionFileUri` in `param.json` is cleared; and `attribute3` is set to 0. Three toggles in the advanced options ("PlayGo fix" box), plus CLI `--keep-playgo`, `--keep-version-uri`, `--keep-attribute3` to opt out of any of them. Note on `attribute3`: no public documentation maps a PlayGo bit in that field, so the whole field is set to 0 exactly like unchecking every flag in a PARAM editor — the documented flags (video-out info, Share Library Capture API, HFR, High Framerate Mode, Auto Scaling) are switched off in the package too.
- Works for **app folders, `.exfat` and `.ffpfsc` images and GP5 projects**. Folders and extracted images have the files moved aside during the build; a Dokan virtual drive hides them and overlays the patched `param.json` without touching the image; a read-only `hdiutil` mount now extracts instead, because it cannot hide anything; GP5 projects have the `.gp5` file rewritten temporarily (exclude masks for the Normal layout, the explicit `<file>` entries dropped for the Flat layout). Every source file, and the `.gp5` itself, is restored byte-for-byte afterwards — also when the build fails or is cancelled.
- `fakelib/libScePlayGo.sprx` is **never touched by this tool**. Verified on a real package: the LibProsperoPkg engine drops that module (and `fakelib/libSceAmpr.sprx`) from `fakelib` by itself, while every other module — and the copies in `fakelib2` — are packaged normally. No option here changes that.
- **`fakelib/libScePlayGo.sprx` and `fakelib/libSceAmpr.sprx` are removed by the LibProsperoPkg engine itself**, with no option to turn that off — verified by disassembling `ProsperoPkgBuilder.BuildInnerTree`, which finds the first directory named `fakelib` and calls `Files.RemoveAll(f => f.name == "libSceAmpr.sprx" || f.name == "libScePlayGo.sprx")`. Fourteen ways of feeding those files in (plain folder, GP5 Flat explicit entry, renamed source directory, letter-case variants, duplicated `fakelib` entries, `<dir>` plus `<file>` shapes) were all removed; only other paths survive (`sce_module/`, `fakelib2/`, `fakelib/<subdir>/`). This tool never touches those files — the removal is the engine's own behaviour.
- **AMPR emu index.** The engine strips `fakelib/libSceAmpr.sprx` (needed for backport and dlc_emu, per its author) but kept `ampr_emu.index` ("I forgot about the index"). The app now removes that leftover as well — advanced toggle "Clean up AMPR emu leftovers" (on), CLI `--keep-ampr`; `fpkg-cli inspect` prints an `AMPR:` line.
- **DLC packages from `dlc_emu.ini`.** Drakmor's DLC emulator (`dlc_emu.ini` + `libSceAppContent` / `libSceNpEntitlementAccess` / `libSceGameUpdate` in `fakelib`) is **kept in the package** — it is not what caused the black screen. When a source carries one, the source card offers to build a real DLC package for every entry in that game's `dlc_emu.ini` (about 0.7 MB each, each with its own RIF `license.dat` / `license.info`; built as *additional content with data* over an empty data folder because the entitlement-only form is a bare CNT the PS5 debug installer refuses), so the DLC also unlocks through the normal path. CLI: `fpkg-cli dlc-from-ini <folder|image|dlc_emu.ini> --output <dir>`; `inspect` lists the entries. An advanced toggle (`--strip-dlc-emu`) removes the emulator from the package instead, dropping a `fakelib` left empty by the cleanup. Extracted entitlement-only DLC packages are labelled "DLC — entitlement only, no data (installable)" instead of "metadata only".
- **Build panel checkboxes.** "DRM standard" and "Drop old playgo*" sit right next to the Build button; both are saved with the rest of the settings. New **Restore defaults** link puts every packaging option back to its default value after a confirmation — paths, package info read from the source, language, theme, history and window size are kept.
- **Same-named package in the output folder now asks first**: overwrite, keep the old one (renamed to `… (1).pkg`, numbered upwards) or cancel — in the app, and in `fpkg-cli` via a console prompt or `--overwrite` / `--keep-existing`. Previously the old package was silently replaced.
- Tests: 191, including a full snapshot of the source tree (paths, sizes, SHA-256, timestamps) asserted identical after successful, cancelled, failed and GP5 builds.

## 2.1.6 — 2026-09-14

- **Windows mounts images too — no more 149 GB extraction.** A read-only virtual drive (Dokan) served by the app's own exFAT reader exposes the app folder of an `.exfat` **or `.ffpfsc`** image; the packager reads straight from the image, exactly like `hdiutil` on macOS. Junk files are hidden on the drive and the DRM fix (`applicationDrmType` → `standard`) is applied on the virtual drive, so nothing is ever written to the image and nothing needs to be extracted. The strategy combo now also appears for `.ffpfsc` sources on Windows; the disk estimate drops the extraction copy when mounting is possible.
- **Windows installer (`…-Windows-x64-Setup.exe`, NSIS).** Installs the app and `fpkg-cli` to Program Files with Start-menu/desktop shortcuts and an uninstaller, and installs the bundled Dokan driver in the same run (silent `msiexec`, one administrator prompt) — nothing else to download or install; Vietnamese/English setup UI. The portable zip stays available and **enables the driver by itself on first launch** (unmodified `Dokan_x64.msi` 2.3.1.1000, LGPL/MIT, in `app/redist/`; only the Windows UAC prompt is shown; declining leaves "Enable direct mounting" in the advanced options). `fpkg-cli install-dokan` does the same on the command line. Installed copies update through the Setup.exe asset, portable copies through the zip.
- **Components & plugins box** (top of the advanced options, also in `fpkg-cli info`): checks for real that the LibProsperoPkg engine, the PS5 debug keys, the built-in Kraken encoder, native Oodle (`libScePubTools.dll`), copy-free mounting (hdiutil / Dokan driver actually running) and the sleep guard work, with a green/amber/red dot per row, "Check again" and "Copy report".
- Kraken level row: the description no longer overlaps the label.
- Tests: 144 (virtual file system listing/reading/overlay/junk/access and component-probe tests run on every OS without the driver).

## 2.1.5 — 2026-09-14

- **LibProsperoPkg from fpkg-gui 0.6.5.** Its "force the DRM mode to standard" fix is implemented in the app: while building, `applicationDrmType` in `sce_sys/param.json` is set to `"standard"` (packages built with `"free"` DRM show a lock on the PS5 and refuse to start); the source file is restored byte-for-byte afterwards, read-only exFAT images are extracted instead of mounted when they need it. Advanced toggle "Force DRM standard" (on by default), CLI `--keep-drm` to opt out.
- **Disk-full recovery** (new library API): when the temp or output disk fills up, the build pauses and asks you to free space and retry instead of failing; CLI prompts on the console.

## 2.1.4 — 2026-09-14

- **Check for updates.** The app checks GitHub Releases every time it starts (can be turned off in the advanced options) and shows a green "New version vX.Y.Z" badge that opens the download, puts a dot on the ↻ button and asks whether to download it on every start; the ↻ button checks manually; CLI `fpkg-cli check-update`.
- **`.ffpfsc` sources.** A `.ffpfsc` file is a PS5 PFS container (superblock v2, 64 KiB blocks) holding one PFSC-compressed exFAT image of the game; the app now opens it like an `.exfat` image — the exFAT reader sits on top of the library's PFSC decompression (~900 MB/s, no intermediate file), metadata / icon / size / junk detection work directly, and building extracts the app folder to the temp folder (mounting is not possible). GUI: separate ".exfat image" and ".ffpfsc file" buttons plus drag & drop; CLI: `inspect` / `build --source x.ffpfsc`.

## 2.1.3 — 2026-09-14

- Version badge next to the app name in the header and in the window title.
- "Clear history" link at the end of the recently used sources row (forgets the paths only).

## 2.1.2 — 2026-09-14

- **Output folders follow the source automatically.** New "Auto from source" (build) and "Auto from package" (extract) toggles, on by default: while on, the output folder is always "<source>-pkg" / "<package>-extract" next to the source and updates the moment you pick another source (browse, drop, recent, typed path); the field is locked. Switch it off to choose your own folder (picking a folder in extract mode switches it off automatically). The choice is remembered.
- Verified with a 30,005-file / 352 MB folder: scan 0.12 s, full build 14 s, listing the resulting package 0.5 s — file count barely matters, data size does.

## 2.1.1 — 2026-09-14

- **Extracted app trees are now in Sony layout.** The "original app tree" option merges param.json, icon0.png, pic0.png and the playgo files from the CNT into `sce_sys/`, so the output folder can be used directly as a source to build an FPKG again (CLI: `pkg-extract` does the same; `--no-sce-sys` skips it, `--cnt` still exports the raw CNT tables to `cnt/`).
- Unhandled exceptions on the UI thread are logged to `error.log` and shown in the build log instead of terminating the app; unobserved task exceptions are logged.
- Verified on a 36 GB / 166,707-file package (ASTRO BOT): info 0.16 s, listing 3.8 s, extraction ~200 MB/s.
- The default preset is now simply called **Standard** ("Level 4 · recommended"); the explanation of its equivalence with the old Kraken 7 moved to the detail text.
- "Open output folder" / "Open with external app" now use the OS command (Finder `open`, Explorer, `xdg-open`) first — fixes the button doing nothing on macOS.
- The extract-mode output folder follows the newly opened package again when it was only the previous package's automatic "<name>-extract" suggestion.

## 2.1.0 — 2026-09-13

Updated to the latest **LibProsperoPkg** (from `fpkg-gui 0.6.2`) and exposed its new capabilities.

### Library (LibProsperoPkg)
- Faster large-package builds through overlapping reads, compression and writes, with fewer intermediate copies.
- Block deduplication and an in-build compression cache.
- Improved built-in Kraken compression, especially at levels 8–9.
- PFS v2 / v3 selection, configurable compression block size and automatic shuffle selection for PFS v3.
- Automatic fallback to the built-in Kraken encoder when Oodle Reduced is unavailable.
- Improved progress reporting (byte throughput), reduced log verbosity.
- File-handling and build-cancellation fixes; existing packages are preserved if a rebuild fails.

### Measured on a 21.3 GB real game (macOS, Apple Silicon, built-in Kraken)
| Library | Time | Package |
|---|---|---|
| LibProsperoPkg shipped with 2.0.0, "Kraken 7" (= Normal encoder) | 3 min 40 s | 9.03 GiB |
| LibProsperoPkg shipped with 2.1.0, **Standard (Kraken 4, default)** | **2 min 13 s** | **8.86 GiB** (−1.9 %) |
| LibProsperoPkg shipped with 2.1.0, Smallest (Kraken 7 Optimal) | 12 min 49 s | 8.54 GiB (−5.5 %) |

Side by side on the same 400 MB set the 2.0.0 engine produced identical packages at levels 4 and 7 (its "level 7" was the Normal encoder) and the 2.1.0 engine at level 4 reproduces that result at the same speed, so **Standard** keeps the old size and speed while **Smallest** is a genuinely new, slower Optimal mode.

### App / CLI
- New advanced options: **PFS compression format (v2 / v3)**, **Kraken block size (128–256 KiB)**, **pre-compression shuffle pattern**, **automatic shuffle analysis**, **skip PFS input check** (expert), **physical layout optimisation**.
- **Presets re-mapped to the new encoder.** The 2.0.0 engine used the same Normal encoder for levels 4–7 (identical output at level 4 and 7); in 2.1.0 level 4 reproduces that result at the same speed while level 7 is a new, ~5–6× slower *Optimal* mode. The default preset is therefore **Standard = Kraken 4** (the old "Kraken 7" size and speed, plus dedup/layout gains; CLI id `balanced`, aliases `sony` / `standard`); **Smallest = Kraken 7 Optimal** (~3 % smaller, 5–6× slower) stays available.
- New **Maximum** preset (Kraken 9 + PFS v3 + shuffle analysis) next to Fast / Standard / Smallest. Per the library author: the PFS v3 shuffle mechanism optimises texture compression, is very time-consuming and only fully effective at level 9, and PFS v3 packages need PS5 firmware 7.00 or newer — the app shows these warnings in the advanced options and the build log.
- Build panel shows the live **throughput** (MiB/s) next to the ETA; progress model covers the new planning and layout phases.
- Smoother progress on large games: the inner-compression phase is now estimated from the library’s per-file lines (byte-weighted), so the bar moves every few seconds instead of once per multi-GB file; phase weights re-tuned from a 21 GB real-game build.
- New `PSVIETHOA_SCROLL=<pixels>` dev hook for documentation screenshots.
- CLI: `--pfs`, `--block-size`, `--shuffle`, `--shuffle-analysis`, `--shuffle-prediction-level`, `--skip-pfs-input-check`, `--no-layout-optimization`, `--source-mode`, `--project` (GP5).
- New **Extract PKG** mode (Build | Extract mode bar under the header): inspect a `.pkg` (FIH / CNT headers, region map, every `param.json` field, icon, sce_sys entries) and list the files of the inner PPR-PFS image; filter, tick files/folders and extract a selection or the whole package with progress and cancel; export `sce_sys` / SI entries; optional SHA-256. PLAINTEXT_NOAUTH packages are decoded in place in 4 MiB chunks (LRU cache, no temp image); Native (AES-XTS) packages are decrypted to the temp folder first; retail packages are information-only. CLI: `pkg-info`, `pkg-list`, `pkg-extract` (`--include` globs, `--cnt`, `--threads`). Dev hooks `PSVIETHOA_MODE=extract`, `PSVIETHOA_EXTRACT_PKG=<pkg>`, `PSVIETHOA_EXTRACT_PREVIEW=<path or file name inside the package>`, `PSVIETHOA_AUTOEXTRACT=1`.
- **GP5 project as a third source type** (`.gp5`, mirroring fpkg-gui's Folder | GP5 selector): ".gp5 project" button in step 1 and drag & drop; metadata, size and validation are read through the project (Normal or Flat layout, `global_exclude` / `dir_exclude` / `file_exclude` masks, relative paths resolved from the `.gp5` folder); the project's passcode pre-fills the passcode field; new advanced **Source reading mode** (automatic top-level `.gp5` / folder only) for folder sources; `fpkg-cli inspect` and `build --source x.gp5`.
- New PSVIETHOA logo (`docs/logo.svg`): app icon on macOS / Windows, window header and README banner are generated from it (`scripts/make-icons.py`, `scripts/make-banner.py`).
- Credits shown in the header, footer, help window and CLI.
- Fixed: runtime language switch now updates every label; help window no longer overflows horizontally.

## 2.0.0 — 2026-09-13
- First cross-platform release (macOS Apple Silicon / Intel, Windows x64) — C# / .NET 10 + Avalonia.
- Bilingual UI (Vietnamese / English), `.exfat` disk-image sources, speed presets, free-space & junk checks, ETA, verification, `fpkg-cli`.
